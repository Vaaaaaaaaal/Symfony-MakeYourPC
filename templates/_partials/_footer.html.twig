<footer class="site-footer">
    <div class="container">
        <div class="footer-content">
            <div class="footer-info">
                <img src="{{ asset('images/Logo.svg') }}" alt="Logo MakeYourPC" class="footer-logo">
                <p>&copy; {{ "now"|date("Y") }} MakeYourPC. Tous droits réservés.</p>
            </div>
            <div class="footer-links">
                <h3>Liens rapides</h3>
                <ul>
                    <li><a href="{{ path('app_home') }}">Accueil</a></li>
                    <li><a href="#">Produits</a></li>
                    <li><a href="#">À propos</a></li>
                    <li><a href="#">Contact</a></li>
                </ul>
            </div>
            <div class="footer-newsletter">
                <h3>Restez informé</h3>
                <p>Inscrivez-vous à notre newsletter pour recevoir nos dernières offres et nouveautés.</p>
                {{ form_start(newsletter_form, {'attr': {'class': 'newsletter-form'}}) }}
                    {{ form_widget(newsletter_form.email, {
                        'attr': {
                            'placeholder': 'Votre adresse e-mail',
                            'required': true
                        }
                    }) }}
                    <button type="submit">S'inscrire</button>
                {{ form_end(newsletter_form) }}
            </div>
        </div>
    </div>
</footer>
