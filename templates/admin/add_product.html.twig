{% extends 'base.html.twig' %}

{% block title %}Ajouter un produit - MakeYourPC{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="my-4">Ajouter un produit</h1>
        
        <div class="add-product-form">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <button type="submit" class="btn btn-primary">Ajouter le produit</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
